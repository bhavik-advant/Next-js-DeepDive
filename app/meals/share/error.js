"use client"
export default function ErrorPage({error}) {
    return (
        <main className="error">
            <h1>AN error occurred!</h1>
            <p>Invalid Input.</p>
        </main>
    )
}